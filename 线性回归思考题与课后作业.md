# 线性回归思考题与课后作业

## 1. 代数证明题

**题目：** 请推导证明一元线性回归中，残差之和必然等于零（\(\sum_{i=1}^n e_i = 0\)），且残差与自变量乘积之和也必然等于零（\(\sum_{i=1}^n x_i e_i = 0\)）。

**证明：**

一元线性回归模型为 \(y_i = \beta_0 + \beta_1 x_i + \epsilon_i\)，其预测方程为 \(\hat{y}_i = \hat{\beta}_0 + \hat{\beta}_1 x_i\)。
残差定义为 \(e_i = y_i - \hat{y}_i = y_i - (\hat{\beta}_0 + \hat{\beta}_1 x_i)\)。

普通最小二乘法（OLS）的目标是最小化残差平方和（RSS）：
\[
RSS = \sum_{i=1}^n e_i^2 = \sum_{i=1}^n (y_i - \hat{\beta}_0 - \hat{\beta}_1 x_i)^2
\]

为了使RSS最小，我们需要对 \(\hat{\beta}_0\) 和 \(\hat{\beta}_1\) 分别求偏导，并令其等于0。

**(1) 证明 \(\sum_{i=1}^n e_i = 0\)**
对 \(\hat{\beta}_0\) 求偏导：
\[
\frac{\partial RSS}{\partial \hat{\beta}_0} = -2 \sum_{i=1}^n (y_i - \hat{\beta}_0 - \hat{\beta}_1 x_i) = 0
\]
化简得：
\[
\sum_{i=1}^n (y_i - \hat{\beta}_0 - \hat{\beta}_1 x_i) = 0
\]
即：
\[
\sum_{i=1}^n e_i = 0
\]
**得证。**

**(2) 证明 \(\sum_{i=1}^n x_i e_i = 0\)**
对 \(\hat{\beta}_1\) 求偏导：
\[
\frac{\partial RSS}{\partial \hat{\beta}_1} = -2 \sum_{i=1}^n x_i (y_i - \hat{\beta}_0 - \hat{\beta}_1 x_i) = 0
\]
化简得：
\[
\sum_{i=1}^n x_i (y_i - \hat{\beta}_0 - \hat{\beta}_1 x_i) = 0
\]
即：
\[
\sum_{i=1}^n x_i e_i = 0
\]
**得证。**

---

## 2. 理解思考题

**题目：** 为什么在多元回归中，往模型添加一个纯随机生成的无关特征，\(R^2\) 会上升或保持不变，而 \(R^2_{adj}\) 会下降？请用自由度与 RSS 变动解释。

**解答：**

**1. 关于 \(R^2\) 的变化：**
\(R^2\) 的计算公式为 \(R^2 = 1 - \frac{RSS}{TSS}\)。
在普通最小二乘法（OLS）中，每增加一个自变量（即使该变量与因变量完全无关），模型参数就多了一个，模型在训练集上的拟合能力“理论上”不会变差。OLS算法会尽量利用这个新变量去减小残差平方和（RSS）。
因此，增加新特征后，\(RSS_{new} \le RSS_{old}\)。
由于总平方和（TSS）只取决于因变量 \(y\)，不随特征增加而改变，所以 \(\frac{RSS}{TSS}\) 会减小或不变，导致 \(R^2\) 必然上升或保持不变。这体现了 \(R^2\) 的一个缺陷：它奖励模型的复杂性，即使增加的是噪声。

**2. 关于 \(R^2_{adj}\) 的变化：**
调整后的 \(R^2_{adj}\) 公式为：
\[
R^2_{adj} = 1 - \frac{RSS / (n - p - 1)}{TSS / (n - 1)}
\]
其中 \(n\) 是样本量，\(p\) 是特征数量。

*   **RSS 变动：** 增加一个随机特征，RSS 可能只会极其微小地减小，甚至几乎不变（因为该特征对解释 \(y\) 没有实际帮助）。
*   **自由度变动：** 分母中的自由度 \((n - p - 1)\) 会随着 \(p\) 的增加而减小。这意味着对模型复杂度的“惩罚”变大了。

**结论：**
当增加一个无关特征时，RSS 的减小幅度（分子减小）通常远小于自由度减少带来的惩罚（分母减小）。这导致 \(\frac{RSS / (n - p - 1)}{TSS / (n - 1)}\) 这一项整体变大，从而使得 \(1 - \text{该项}\) 变小。
因此，\(R^2_{adj}\) 会下降，这正确地反映了该特征对模型预测能力没有贡献，反而增加了过拟合风险。